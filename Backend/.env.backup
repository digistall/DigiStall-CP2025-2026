# Database Configuration - DOCKER VERSION
DB_HOST=database
DB_PORT=3306
DB_USER=digistall_user
DB_PASSWORD=digistall_password
DB_NAME=naga_stall

# JWT Configuration - IMPORTANT: Change this in production!
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-this-in-production

# Server Configuration
WEB_PORT=3001
MOBILE_PORT=5001

# Environment
NODE_ENV=development

# Email Configuration (For Gmail)
# To enable actual email sending:
# 1. Set NODE_ENV=production (or comment out NODE_ENV=development)
# 2. Create an App Password in your Gmail account:
#    - Go to https://myaccount.google.com/apppasswords
#    - Generate a new app password for "Mail"
# 3. Fill in the values below:
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password-here
FROM_EMAIL=noreply@nagastallmanagement.com
FROM_NAME=Naga Stall Management System
APP_BASE_URL=http://localhost:5174

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:3000,http://localhost