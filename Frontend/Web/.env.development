# ===== DEVELOPMENT ENVIRONMENT VARIABLES =====
# These are used during local development with Vite dev server

# Use relative path to leverage Vite's proxy configuration
# The proxy will forward /api/* requests to http://localhost:3001/api/*
VITE_API_URL=/api
